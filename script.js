// // Minimal JS: Log a message when the page is loaded
// window.addEventListener('DOMContentLoaded', () => {
//     console.log("Country gallery is ready!");
    
//     // Optional: Add a simple click alert for cards
//     const cards = document.querySelectorAll('.card');
//     cards.forEach(card => {
//         card.addEventListener('click', () => {
//             const name = card.querySelector('.card-name').innerText;
//             alert("You clicked on " + name);
//         });
//     });
// });
// This script is optional. 
// It currently does nothing to keep your site lightweight.
console.log("Gallery loaded with fixed image sizes.");