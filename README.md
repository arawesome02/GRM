# GRM
📁 Project StructureThe project is split into three main files for better organization:FilePurposeindex.htmlDefines the content and the 12 country card containers.style.cssHandles the layout (CSS Grid), card sizing, and image "fixing".script.jsProvides a lightweight entry point for future interactive features.
